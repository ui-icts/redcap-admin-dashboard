{
  "name": "Admin Dashboard",
  "description": "Provides a sortable table view of various reports on REDCap metadata (users, projects, etc). Includes a number of built-in reports as well as support for custom SQL queries.",
  "documentation": "README.md",

  "namespace": "UIOWA\\AdminDash",


  "authors": [
    {
      "name": "Izzy Neuhaus",
      "email": "isabelle-neuhaus@uiowa.edu",
      "institution": "University of Iowa Institute for Clinical and Translational Science"
    }
  ],

  "links": {
    "control-center": [
      {
        "name": "Admin Dashboard",
        "icon": "table",
        "url": "index.php"
      }
    ]
  },

  "permissions": [
    "redcap_module_system_change_version",
    "redcap_data_entry_form",
    "redcap_save_record"
  ],

  "system-settings": [
    {
      "key": "config-pid",
      "name": "Project ID of report configuration project (must also have module enabled at project-level",
      "type": "project-id",
      "required": false
    },
    {
      "key": "use-api-urls",
      "name": "Use versionless URLs for easier bookmarking (disable to fix issues with Shibboleth authentication)",
      "type": "checkbox"
    },
    {
      "key": "test-query-timeout",
      "name": "Timeout (in milliseconds) for running test queries (set to 0 for no timeout)",
      "type": "text",
      "default": "3000"
    },
    {
      "key": "config-defaults",
      "name": "<h4>Default settings when creating new reports:</h4>",
      "type": "descriptive"
    },
    {
      "key": "auto_header_format___1",
      "name": "Replace underscores with spaces in column names",
      "type": "checkbox"
    },
    {
      "key": "auto_header_format___2",
      "name": "Capitalize words in column names",
      "type": "checkbox"
    },
    {
      "key": "auto_header_format___3",
      "name": "Remove '_GROUP' suffixes in column names",
      "type": "checkbox"
    },
    {
      "key": "auto_link_format",
      "name": "Auto-format known value types (e.g. project_id, username, email, etc) into links",
      "type": "checkbox"
    },
    {
      "key": "auto_code_format",
      "name": "Auto-format common coded values (e.g. display project_status '0' as 'Development') into labels",
      "type": "checkbox"
    }
  ]
}
